import { assert } from '@blast-engine/utils'
import { responsiveComponent } from './responsive-component'

describe('responsiveComponent()', () => {

  it(`is a function.`, () => {

    assert([ typeof responsiveComponent === 'function' ])

  })

})